-- This script computes the score average of all records in second_table
-- of the database hbtn_0c_0
SELECT AVG(id) AS average FROM second_table;
-- SELECT (SUM(id) / COUNT(*)) AS average FROM second_table;
